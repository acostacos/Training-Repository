var a

// goes to internet and looks for a value

// Attempts to convert a to a boolean
// a === 0 handles the case if the variable accepts 0 as a valid value
if(a || a === 0){
    console.log('Something is there.')
}