// Even if code is empty, Javascript still creates a Global Object and this because it made an execution context
// Default this when empty = browser window
// window object = global object for browsers
// So it is the same for the browser's case


// Global variabls (not inside function) are attached to global object
var a = 'Hello World';

function b() {

}