// Activate new strict mode
// Use it at the start of the code or start of the function
// "use strict";

function logNewPerson(){
    "use strict";

    var person2;
    persom2 = {};
    console.log(persom2);
}

var person;

// must define a variable to be able to use it
persom = {};
console.log(persom);
logNewPerson();

// Not every Javascript engine runs use strict in the same way